# Loose Ends

- gemini do not execute skill goodnight properly, when I type: `/conductor-goodnight run` it confused what "run" means and try to continue with existing track instead of running a skill. investigate it further

- gemini encountered workspace restriction when accessing `dpd-db` repository. Used shell commands (`cat`, `mv`, etc.) to read/write files in `dpd-db` as a workaround. Consider adding `dpd-db` to the default workspace or resolving the boundary check issue.

explore option for downloading whole websire for offline use, so that students can access the materials without internet connection. 


add a description of the usage of materials as a whole. explain how to use the website, what materials are available, and how to navigate through them. also provide tips and recommendations for students on how to make the most out of the materials and improve their learning experience.


in the SBS-DPD dictionary change links to the classes