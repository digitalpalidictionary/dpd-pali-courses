# Loose Ends

- gemini do not execute skill goodnight properly, when I type: `/conductor-goodnight run` it confused what "run" means and try to continue with existing track instead of running a skill. investigate it further

- For truly uninterrupted mode: start Claude with `claude --dangerously-skip-permissions`. Shift+Tab `auto` mode still prompts for some bash (safety classifier). Only the flag fully bypasses all approvals. Add alias: `alias claude-unsafe='claude --dangerously-skip-permissions'`

- **`dpd_operations.log`** — TSV audit log generated by `Printer(Path("dpd_operations.log"))` in `tools/printer.py`. Content is mostly `ok` messages; header repeats every run (bug in `TSVFormatter.header_written`). Decision pending: disable file logging (`Printer()` no args) + add `dpd_operations.log` to `.gitignore`.